<Application x:Class="SnippingTool.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SnippingTool"
             xmlns:tb="http://www.hardcodet.net/taskbar">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Control templates — always loaded; use DynamicResource for colors -->
                <ResourceDictionary Source="pack://application:,,,/Themes/Controls.xaml"/>
                <!-- Active theme dictionary is loaded at runtime by ThemeService -->
            </ResourceDictionary.MergedDictionaries>

            <tb:TaskbarIcon x:Key="TrayIcon"
                            IconSource="/Assets/icon.ico"
                            ToolTipText="Snipping Tool"
                            TrayLeftMouseUp="TrayIcon_LeftClick">
                <tb:TaskbarIcon.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="New Snip" Click="NewSnip_Click" />
                        <MenuItem Header="Open image..." Click="OpenImage_Click" />
                        <Separator />
                        <MenuItem Header="Settings" Click="Settings_Click" />
                        <MenuItem Header="Check for Updates" Click="CheckForUpdates_Click" />
                        <MenuItem Header="About" Click="About_Click" />
                        <Separator />
                        <MenuItem Header="Exit" Click="Exit_Click" />
                    </ContextMenu>
                </tb:TaskbarIcon.ContextMenu>
            </tb:TaskbarIcon>
        </ResourceDictionary>
    </Application.Resources>
</Application>
